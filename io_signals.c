#include "shell.h"

void setup_signals(void) {
    // Temporary stub — Member 5 replaces this
}

void execute_with_redirection(Command *cmd) {
    // Temporary stub — Member 5 replaces this
    printf("[io stub] redirection not yet implemented\n");
}

void execute_pipe(char *input) {
    // Temporary stub — Member 5 replaces this
    printf("[pipe stub] pipe detected: %s\n", input);
}
